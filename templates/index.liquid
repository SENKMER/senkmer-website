{% comment %} Minimal home template for Shopify theme detection {% endcomment %}
<section class="page-width">
  <header class="section-header">
    <h1>{{ shop.name }}</h1>
  </header>
  <div class="rte">
    <p>Welcome to our store. This is a minimal template to allow theme publishing.</p>
  </div>
</section>
